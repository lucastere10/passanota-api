substitutions:
  # Infra
  _AR_HOSTNAME: us-central1-docker.pkg.dev
  _DEPLOY_REGION: us-central1
  _AR_REPO: passanota-images
  _SERVICE_NAME: passanota-api
  _RUNTIME_SA: passanota-sa
  _IMAGE: ${_AR_HOSTNAME}/${PROJECT_ID}/${_AR_REPO}/${_SERVICE_NAME}
  _CONCURRENCY: "20"
  _LABEL_APP: passanota
  _LABEL_ENV: production
  _LABEL_OWNER: lucas

  # Secrets
  _SECRET_DATABASE_URL: passanota-database-url
  _SECRET_SUPABASE_KEY: passanota-supabase-secret-key
  _SECRET_LLM_API_KEY: passanota-openai-api-key
  _SECRET_RESEND_API_KEY: passanota-resend-api-key

  # Application
  _SUPABASE_URL: https://kdmiptpxatsobroqzjnq.supabase.co
  _FRONTEND_URL: "https://passanota.caldasdev.store"
  _LLM_PROVIDER: openai
  _LLM_MODEL: gpt-5.4
  _EMAIL_FROM: "Lucas Caldas <noreply@caldasdev.store>"
  _PLATFORM_ADMIN_EMAIL: "lucastere10@gmail.com"
  _PLATFORM_ADMIN_NOTIFY_EMAIL: "lucastere10@gmail.com"

  # Cloud Tasks
  _CLOUD_TASKS_ENABLED: "true"
  _GCP_LOCATION: us-central1
  _CLOUD_TASKS_INVOICE_QUEUE: invoice-processing
  _CLOUD_TASKS_EMAIL_QUEUE: email-delivery
  _TASK_HANDLER_BASE_URL: "https://passanota-api-elokeq3cta-uc.a.run.app"
  _CLOUD_TASKS_SERVICE_ACCOUNT: "cloud-tasks-sa@caldas-projects-dev.iam.gserviceaccount.com"
  _MIN_INSTANCES: "0"
  _WEB_RUNTIME_SA: passanota-sa

steps:
  - id: qa
    name: python:3.13-slim
    entrypoint: bash
    args:
      - -c
      - |
        set -euo pipefail
        pip install --no-cache-dir uv
        uv pip install --system ".[dev]"
        ruff check app tests
        pytest tests/ -q --ignore=tests/test_supabase_integration.py

  - id: build
    name: gcr.io/cloud-builders/docker
    env:
      - DOCKER_BUILDKIT=1
    args:
      - build
      - --cache-from
      - ${_IMAGE}:latest
      - -t
      - ${_IMAGE}:${COMMIT_SHA}
      - -t
      - ${_IMAGE}:latest
      - -f
      - Dockerfile
      - .
    waitFor: [qa]

  - id: push
    name: gcr.io/cloud-builders/docker
    args:
      - push
      - --all-tags
      - ${_IMAGE}
    waitFor: [build]

  - id: deploy
    name: gcr.io/google.com/cloudsdktool/cloud-sdk:slim
    entrypoint: gcloud
    args:
      - run
      - deploy
      - ${_SERVICE_NAME}
      - --image
      - ${_IMAGE}:${COMMIT_SHA}
      - --region
      - ${_DEPLOY_REGION}
      - --platform
      - managed
      - --no-allow-unauthenticated
      - --port
      - "8080"
      - --memory
      - 2Gi
      - --cpu
      - "2"
      - --timeout
      - "300"
      - --cpu-boost
      - --concurrency
      - ${_CONCURRENCY}
      - --revision-suffix
      - ${SHORT_SHA}
      - --labels
      - app=${_LABEL_APP},env=${_LABEL_ENV},owner=${_LABEL_OWNER}
      - --execution-environment
      - gen2
      - --service-account
      - ${_RUNTIME_SA}@${PROJECT_ID}.iam.gserviceaccount.com
      - --min-instances
      - ${_MIN_INSTANCES}
      - --max-instances
      - "10"
      - --startup-probe
      - httpGet.path=/health/live,httpGet.port=8080,initialDelaySeconds=5,timeoutSeconds=5,periodSeconds=5,failureThreshold=24
      - --set-env-vars
      - ^|^SUPABASE_URL=${_SUPABASE_URL}|FRONTEND_URL=${_FRONTEND_URL}|LLM_PROVIDER=${_LLM_PROVIDER}|LLM_MODEL=${_LLM_MODEL}|EMBEDDINGS_ENABLED=true|SUPABASE_STORAGE_BUCKET=invoice-photos|DEBUG=false|EMAIL_FROM=${_EMAIL_FROM}|PLATFORM_ADMIN_EMAIL=${_PLATFORM_ADMIN_EMAIL}|PLATFORM_ADMIN_NOTIFY_EMAIL=${_PLATFORM_ADMIN_NOTIFY_EMAIL}|PYTHONUNBUFFERED=1|CLOUD_TASKS_ENABLED=${_CLOUD_TASKS_ENABLED}|GCP_PROJECT=$PROJECT_ID|GCP_LOCATION=${_GCP_LOCATION}|CLOUD_TASKS_INVOICE_QUEUE=${_CLOUD_TASKS_INVOICE_QUEUE}|CLOUD_TASKS_EMAIL_QUEUE=${_CLOUD_TASKS_EMAIL_QUEUE}|TASK_HANDLER_BASE_URL=${_TASK_HANDLER_BASE_URL}|CLOUD_TASKS_SERVICE_ACCOUNT=${_CLOUD_TASKS_SERVICE_ACCOUNT}
      - --set-secrets
      - DATABASE_URL=${_SECRET_DATABASE_URL}:latest,SUPABASE_SECRET_KEY=${_SECRET_SUPABASE_KEY}:latest,LLM_PROVIDER_API_KEY=${_SECRET_LLM_API_KEY}:latest,RESEND_API_KEY=${_SECRET_RESEND_API_KEY}:latest
    waitFor: [push]

  - id: grant-invokers
    name: gcr.io/google.com/cloudsdktool/cloud-sdk:slim
    entrypoint: bash
    args:
      - -c
      - |
        set -euo pipefail
        grant() {
          gcloud run services add-iam-policy-binding ${_SERVICE_NAME} \
            --region=${_DEPLOY_REGION} \
            --project="$PROJECT_ID" \
            --member="$$1" \
            --role="roles/run.invoker" \
            --quiet
        }
        grant "serviceAccount:${_WEB_RUNTIME_SA}@$PROJECT_ID.iam.gserviceaccount.com"
        grant "serviceAccount:${_CLOUD_TASKS_SERVICE_ACCOUNT}"
    waitFor: [deploy]

images:
  - ${_IMAGE}:${COMMIT_SHA}
  - ${_IMAGE}:latest

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: E2_HIGHCPU_8

serviceAccount: projects/${PROJECT_ID}/serviceAccounts/cloud-build-sa@${PROJECT_ID}.iam.gserviceaccount.com
