# Supabase Postgres (Settings > Database > Connection string)
# Use postgresql+asyncpg:// ou postgresql:// — o driver asyncpg é aplicado automaticamente
DATABASE_URL=postgresql+asyncpg://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# Supabase (Settings > API — chaves novas sb_secret_ / sb_publishable_)
SUPABASE_URL=https://[project-ref].supabase.co
SUPABASE_SECRET_KEY=sb_secret_...
SUPABASE_PUBLISHABLE_KEY=sb_publishable_...
SUPABASE_STORAGE_BUCKET=invoice-photos

# Extração IA via LLM (openai | gemini | anthropic)
LLM_PROVIDER=openai
LLM_PROVIDER_API_KEY=your-llm-api-key
LLM_MODEL=gpt-4o
LLM_MAX_IMAGE_BYTES=5242880

DEBUG=false
# all = um processo local (HTTP + worker). Produção: http | worker
APP_ROLE=all
EMBEDDINGS_ENABLED=true
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSIONS=512

# E-mail (Resend) e autenticação
RESEND_API_KEY=re_...
EMAIL_FROM=noreply@seudominio.com
FRONTEND_URL=http://localhost:3000
PLATFORM_ADMIN_NOTIFY_EMAIL=seu@email.com
PLATFORM_ADMIN_EMAIL=admin@seudominio.com

# Cloud Tasks (produção GCP — deixe CLOUD_TASKS_ENABLED=false em dev local)
CLOUD_TASKS_ENABLED=false
GCP_PROJECT=caldas-projects-dev
GCP_LOCATION=us-central1
CLOUD_TASKS_INVOICE_QUEUE=invoice-processing
CLOUD_TASKS_EMAIL_QUEUE=email-delivery
TASK_HANDLER_BASE_URL=https://passanota-worker-XXXX.us-central1.run.app
CLOUD_TASKS_SERVICE_ACCOUNT=399951936554-compute@developer.gserviceaccount.com
